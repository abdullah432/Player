<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="priTxtColor">@color/priTxtColor</item>
        <item name="secTxtColor">@color/secTxtColor</item>
        <item name="actionModeMenuIcon">@color/actionModeMenuIcon</item>
        <item name="blackColor">#000000</item>
        <item name="colorControlNormal">@android:color/black</item>
        <item name="buttonPressed">@color/buttonPressed</item>
        <item name="buttonNotPressed">@color/transparent</item>
        <item name="android:colorBackground">@color/backgroundColor</item>

        <item name="windowActionModeOverlay">true</item>
        //Animated activity transition
        <!--<item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>-->
    </style>

    <!--theme for track dialog-->
    <style name="TrackSelectionDialogThemeOverlay" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="windowNoTitle">false</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

</resources>
